# Slim runtime deps for the FastAPI backend (Render).
# The full ML training stack lives in requirements.txt and isn't loaded at request time.
numpy==2.0.2
pandas==2.3.3
fastapi==0.128.8
uvicorn==0.39.0
gtfs-realtime-bindings==1.0.0
slowapi==0.1.9
